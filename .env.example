# ===========================================
# Database (PostgreSQL)
# ===========================================
# For local Docker PostgreSQL
DATABASE_URL="postgresql://streamside:streamside123@localhost:5432/streamside"
DIRECT_URL="postgresql://streamside:streamside123@localhost:5432/streamside"

# For production with external PostgreSQL
# DATABASE_URL="postgresql://user:pass@host:5432/database"
# DIRECT_URL="postgresql://user:pass@host:5432/database"

# ===========================================
# Better Auth
# ===========================================
BETTER_AUTH_SECRET="your-secret-key-change-this-in-production"
BETTER_AUTH_URL="http://localhost:3000"

# Google OAuth (Optional - leave empty to disable)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
# Set to "true" to show Google OAuth button
NEXT_PUBLIC_GOOGLE_OAUTH_ENABLED=""

# ===========================================
# LiveKit (WebRTC)
# ===========================================
# Local development
LIVEKIT_API_KEY="devkey"
LIVEKIT_API_SECRET="secret"
LIVEKIT_WS_URL="ws://localhost:7880"
NEXT_PUBLIC_LIVEKIT_WS_URL="ws://localhost:7880"

# For production (self-hosted on Coolify/VPS):
# 1. Generate secret: openssl rand -base64 32 | tr -d '/+=' | head -c 32
# 2. Use docker-compose.coolify.yml on your VPS
# 3. Set these in Vercel environment variables:
# LIVEKIT_API_KEY="APIStreamside"
# LIVEKIT_API_SECRET="your-generated-secret"
# NEXT_PUBLIC_LIVEKIT_WS_URL="wss://livekit.yourdomain.com" or "wss://YOUR_VPS_IP:7880"

# ===========================================
# Redis
# ===========================================
REDIS_URL="redis://localhost:6379"

# ===========================================
# App
# ===========================================
NODE_ENV="development"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# For production
# NODE_ENV="production"
# NEXT_PUBLIC_APP_URL="https://yourdomain.com"
